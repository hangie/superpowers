{
  "skill_name": "writing-plans",
  "evals": [
    {
      "id": 1,
      "prompt": "Write an implementation plan for splitting a feature branch into two separate PRs. The existing PR contains two unrelated features mixed together. The urgent feature needs its own clean PR against main so it can be reviewed and merged quickly without being held up by the slower, more complex feature that can wait.",
      "expected_output": "A plan capturing intent and constraints. Must not prescribe a specific git strategy before understanding the commit structure. Must identify risks around interleaved commits, shared files, and the existing PR remaining in a working state. Must not contain code blocks, commit messages, or exact git commands.",
      "assertions": [
        { "text": "The output contains a fenced code block opened with ``` followed by a programming language name (python, typescript, javascript, bash, sh, ts, js, or similar)" },
        { "text": "The output contains the exact string 'git commit -m'" },
        { "text": "The output contains 'Expected: PASS' or 'Expected: FAIL' or 'Expected: pass' or 'Expected: fail'" },
        { "text": "The output contains 'git add ' followed by one or more file paths as an explicit instruction" },
        { "text": "The output contains 'Run:' immediately followed by a terminal command on the same line" },
        { "text": "The output contains a CLI flag as part of an explicit run command (e.g. ' -v', ' -f', ' --watch', ' --coverage')" },
        { "text": "The output contains test function definition syntax: 'def test_' or \"it('\" or \"describe('\"" },
        { "text": "The output contains code import syntax: 'import {' or 'from X import' or 'require('" },
        { "text": "The output contains variable declaration syntax: 'const ' or 'let ' or 'var ' followed by a name and '='" },
        { "text": "The output contains a file path with a line number reference in the format filename:NNN (e.g. 'src/utils/git.ts:123')" },
        { "text": "The output contains test assertion code syntax: '.toBe(' or '.toEqual(' or '.toHaveBeenCalled('" },
        { "text": "The output contains a conventional commit message on its own line starting with 'feat:', 'fix:', 'chore:', 'refactor:', or 'test:'" },
        { "text": "The output contains test lifecycle hook syntax: 'beforeEach(' or 'afterEach(' or 'beforeAll('" },
        { "text": "The output contains an expected output prediction after a step (e.g. 'Expected output:' or 'You should see:')" },
        { "text": "The output contains 'npm run ' or 'yarn run ' or 'bun run ' or 'pnpm run ' as an explicit execution instruction in a step" },
        { "text": "The output contains at least one sentence with the word 'goal', 'intent', 'objective', 'purpose', or 'we want' describing the task's aim" },
        { "text": "The output contains at least one use of 'because', 'in order to', or 'so that' explaining why something matters" },
        { "text": "The output contains at least one completion criterion containing 'passes', 'verified', 'no remaining', 'confirmed', or 'complete when'" },
        { "text": "The output contains at least one sentence identifying a risk, constraint, or non-obvious concern using 'risk', 'constraint', 'important', 'note that', 'caveat', or 'careful'" },
        { "text": "The output contains at least one sentence describing what a successful outcome looks like rather than just what steps to perform" },
        { "text": "The output mentions checking or verifying the difference between branches before copying any code" },
        { "text": "The output identifies the risk that the feature branch may contain unrelated changes that should not be copied" },
        { "text": "The output does not contain specific implementation function names prescribed as code to write (e.g. 'clearGitCache', 'getGitStatus', 'getGitAheadBehind')" },
        { "text": "The output mentions cache clearing or cache isolation as a concern for test correctness" },
        { "text": "The output does not contain a specific git show or git checkout command prescribing exactly how to copy code from the branch" },
        { "text": "The output identifies whether commits are interleaved or sequential as something to investigate before choosing a git strategy" },
        { "text": "The output identifies that the new branch should be based off main and not off the existing feature branch" },
        { "text": "The output identifies the risk that both features may touch the same files making a clean split difficult" },
        { "text": "The output identifies that the existing PR must remain in a working state after the split" },
        { "text": "The output does not prescribe a specific git strategy (cherry-pick, rebase, or manual extraction) before understanding the commit structure" }
      ],
      "files": []
    },
    {
      "id": 2,
      "prompt": "Write an implementation plan for migrating a React component library from CSS modules to Tailwind. There are 47 components. Some components have dynamic class logic that will need rethinking, not just find-and-replace. The design tokens currently in CSS variables will need to map to Tailwind config.",
      "expected_output": "A plan capturing intent and constraints. Must not list all 47 components individually. Must identify dynamic class logic and token mapping as distinct concerns. Must not contain code blocks or prescribed Tailwind class replacements.",
      "assertions": [
        { "text": "The output contains a fenced code block opened with ``` followed by a programming language name (python, typescript, javascript, bash, sh, ts, js, or similar)" },
        { "text": "The output contains the exact string 'git commit -m'" },
        { "text": "The output contains 'Expected: PASS' or 'Expected: FAIL' or 'Expected: pass' or 'Expected: fail'" },
        { "text": "The output contains 'git add ' followed by one or more file paths as an explicit instruction" },
        { "text": "The output contains 'Run:' immediately followed by a terminal command on the same line" },
        { "text": "The output contains a CLI flag as part of an explicit run command (e.g. ' -v', ' -f', ' --watch', ' --coverage')" },
        { "text": "The output contains test function definition syntax: 'def test_' or \"it('\" or \"describe('\"" },
        { "text": "The output contains code import syntax: 'import {' or 'from X import' or 'require('" },
        { "text": "The output contains variable declaration syntax: 'const ' or 'let ' or 'var ' followed by a name and '='" },
        { "text": "The output contains a file path with a line number reference in the format filename:NNN (e.g. 'src/components/Button.tsx:45')" },
        { "text": "The output contains test assertion code syntax: '.toBe(' or '.toEqual(' or '.toHaveBeenCalled('" },
        { "text": "The output contains a conventional commit message on its own line starting with 'feat:', 'fix:', 'chore:', 'refactor:', or 'test:'" },
        { "text": "The output contains test lifecycle hook syntax: 'beforeEach(' or 'afterEach(' or 'beforeAll('" },
        { "text": "The output contains an expected output prediction after a step (e.g. 'Expected output:' or 'You should see:')" },
        { "text": "The output contains 'npm run ' or 'yarn run ' or 'bun run ' or 'pnpm run ' as an explicit execution instruction in a step" },
        { "text": "The output contains at least one sentence with the word 'goal', 'intent', 'objective', 'purpose', or 'we want' describing the task's aim" },
        { "text": "The output contains at least one use of 'because', 'in order to', or 'so that' explaining why something matters" },
        { "text": "The output contains at least one completion criterion containing 'passes', 'verified', 'no remaining', 'confirmed', or 'complete when'" },
        { "text": "The output contains at least one sentence identifying a risk, constraint, or non-obvious concern using 'risk', 'constraint', 'important', 'note that', 'caveat', or 'careful'" },
        { "text": "The output contains at least one sentence describing what a successful outcome looks like rather than just what steps to perform" },
        { "text": "The output identifies dynamic class logic as a distinct concern requiring judgment rather than mechanical replacement" },
        { "text": "The output identifies design token mapping or CSS variable mapping as a prerequisite step before component migration begins" },
        { "text": "The output does not enumerate all 47 components individually as separate tasks or steps" },
        { "text": "The output contains a word or phrase indicating grouping or categorisation of components (e.g. 'group', 'categorise', 'batch', 'phase', 'tier', 'type')" },
        { "text": "The output does not contain specific Tailwind utility class names as prescribed replacements (e.g. 'text-sm', 'flex-1', 'bg-blue-500', 'p-4')" }
      ],
      "files": []
    },
    {
      "id": 3,
      "prompt": "Write an implementation plan for adding end-to-end tests for a checkout flow. The checkout has 5 steps: cart review, address, shipping method, payment, confirmation. There is an existing Playwright setup but no checkout tests yet. The payment step uses a third-party iframe that cannot be automated directly.",
      "expected_output": "A plan capturing intent and constraints. Must explicitly identify the payment iframe as a blocker requiring a deliberate strategy. Must not contain Playwright API code. Must mention test data setup and the existing Playwright config.",
      "assertions": [
        { "text": "The output contains a fenced code block opened with ``` followed by a programming language name (python, typescript, javascript, bash, sh, ts, js, or similar)" },
        { "text": "The output contains the exact string 'git commit -m'" },
        { "text": "The output contains 'Expected: PASS' or 'Expected: FAIL' or 'Expected: pass' or 'Expected: fail'" },
        { "text": "The output contains 'git add ' followed by one or more file paths as an explicit instruction" },
        { "text": "The output contains 'Run:' immediately followed by a terminal command on the same line" },
        { "text": "The output contains a CLI flag as part of an explicit run command (e.g. ' -v', ' -f', ' --watch', ' --coverage')" },
        { "text": "The output contains test function definition syntax: 'def test_' or \"it('\" or \"describe('\"" },
        { "text": "The output contains code import syntax: 'import {' or 'from X import' or 'require('" },
        { "text": "The output contains variable declaration syntax: 'const ' or 'let ' or 'var ' followed by a name and '='" },
        { "text": "The output contains a file path with a line number reference in the format filename:NNN (e.g. 'tests/checkout.spec.ts:23')" },
        { "text": "The output contains test assertion code syntax: '.toBe(' or '.toEqual(' or '.toHaveBeenCalled('" },
        { "text": "The output contains a conventional commit message on its own line starting with 'feat:', 'fix:', 'chore:', 'refactor:', or 'test:'" },
        { "text": "The output contains test lifecycle hook syntax: 'beforeEach(' or 'afterEach(' or 'beforeAll('" },
        { "text": "The output contains an expected output prediction after a step (e.g. 'Expected output:' or 'You should see:')" },
        { "text": "The output contains Playwright API calls as prescribed code (e.g. 'page.click', 'page.goto', 'await page', 'expect(page)')" },
        { "text": "The output contains at least one sentence with the word 'goal', 'intent', 'objective', 'purpose', or 'we want' describing the task's aim" },
        { "text": "The output contains at least one use of 'because', 'in order to', or 'so that' explaining why something matters" },
        { "text": "The output contains at least one completion criterion containing 'passes', 'verified', 'no remaining', 'confirmed', or 'complete when'" },
        { "text": "The output contains at least one sentence identifying a risk, constraint, or non-obvious concern using 'risk', 'constraint', 'important', 'note that', 'caveat', or 'careful'" },
        { "text": "The output contains at least one sentence describing what a successful outcome looks like rather than just what steps to perform" },
        { "text": "The output explicitly identifies the third-party payment iframe or payment step as a blocker or constraint" },
        { "text": "The output identifies or asks for a deliberate strategy to handle the untestable payment step (containing 'mock', 'stub', 'skip', 'workaround', or 'alternative')" },
        { "text": "The output mentions test data setup as a concern (e.g. test accounts, test products, test addresses, or seed data)" },
        { "text": "The output mentions reviewing or understanding the existing Playwright configuration or setup before writing tests" },
        { "text": "The output does not prescribe specific Playwright config file changes or specific playwright.config.ts settings" }
      ],
      "files": []
    },
    {
      "id": 4,
      "prompt": "Write an implementation plan for fixing a race condition. The bug report says: 'Occasionally when two users submit a form at the same time, one submission is silently lost. Happens maybe 1 in 50 times. Difficult to reproduce locally.'",
      "expected_output": "A plan that starts with investigation before prescribing a fix. Must not prescribe a specific concurrency solution before diagnosis. Must acknowledge the reproduction difficulty. Must not contain code blocks.",
      "assertions": [
        { "text": "The output contains a fenced code block opened with ``` followed by a programming language name (python, typescript, javascript, bash, sh, ts, js, or similar)" },
        { "text": "The output contains the exact string 'git commit -m'" },
        { "text": "The output contains 'Expected: PASS' or 'Expected: FAIL' or 'Expected: pass' or 'Expected: fail'" },
        { "text": "The output contains 'git add ' followed by one or more file paths as an explicit instruction" },
        { "text": "The output contains 'Run:' immediately followed by a terminal command on the same line" },
        { "text": "The output contains a CLI flag as part of an explicit run command (e.g. ' -v', ' -f', ' --watch', ' --coverage')" },
        { "text": "The output contains test function definition syntax: 'def test_' or \"it('\" or \"describe('\"" },
        { "text": "The output contains code import syntax: 'import {' or 'from X import' or 'require('" },
        { "text": "The output contains variable declaration syntax: 'const ' or 'let ' or 'var ' followed by a name and '='" },
        { "text": "The output contains a file path with a line number reference in the format filename:NNN" },
        { "text": "The output contains test assertion code syntax: '.toBe(' or '.toEqual(' or '.toHaveBeenCalled('" },
        { "text": "The output contains a conventional commit message on its own line starting with 'feat:', 'fix:', 'chore:', 'refactor:', or 'test:'" },
        { "text": "The output contains test lifecycle hook syntax: 'beforeEach(' or 'afterEach(' or 'beforeAll('" },
        { "text": "The output contains an expected output prediction after a step (e.g. 'Expected output:' or 'You should see:')" },
        { "text": "The output contains 'npm run ' or 'yarn run ' or 'bun run ' or 'pnpm run ' as an explicit execution instruction in a step" },
        { "text": "The output contains at least one sentence with the word 'goal', 'intent', 'objective', 'purpose', or 'we want' describing the task's aim" },
        { "text": "The output contains at least one use of 'because', 'in order to', or 'so that' explaining why something matters" },
        { "text": "The output contains at least one completion criterion containing 'passes', 'verified', 'no remaining', 'confirmed', or 'complete when'" },
        { "text": "The output contains investigation or diagnosis steps that appear before any fix implementation steps" },
        { "text": "The output contains at least one sentence describing what a successful outcome looks like rather than just what steps to perform" },
        { "text": "The output prescribes a specific concurrency mechanism as the fix before any diagnosis step (e.g. 'add a mutex', 'use a lock', 'implement a queue', 'use a semaphore') where that prescription appears before investigation tasks" },
        { "text": "The output identifies the difficulty of reproducing the bug as a constraint that needs to be addressed" },
        { "text": "The output contains a definition of what 'fixed' looks like as an observable outcome (not just 'bug is gone')" },
        { "text": "The output acknowledges that the implementation approach may need to change depending on what investigation reveals" },
        { "text": "The output does not contain specific database or locking syntax as prescribed implementation (e.g. 'BEGIN TRANSACTION', 'SELECT FOR UPDATE', 'LOCK TABLE')" }
      ],
      "files": []
    },
    {
      "id": 5,
      "prompt": "Write an implementation plan for a simple change: rename the exported function 'getUserData' to 'fetchUserProfile' across the entire codebase.",
      "expected_output": "A SHORT proportionate plan of no more than 4 tasks. Must not over-engineer a simple rename. Must identify external consumers as a concern. Must not contain code blocks, commit messages, or search commands.",
      "assertions": [
        { "text": "The output contains a fenced code block opened with ``` followed by a programming language name (python, typescript, javascript, bash, sh, ts, js, or similar)" },
        { "text": "The output contains the exact string 'git commit -m'" },
        { "text": "The output contains 'Expected: PASS' or 'Expected: FAIL' or 'Expected: pass' or 'Expected: fail'" },
        { "text": "The output contains 'git add ' followed by one or more file paths as an explicit instruction" },
        { "text": "The output contains 'Run:' immediately followed by a terminal command on the same line" },
        { "text": "The output contains a CLI flag as part of an explicit run command (e.g. ' -v', ' -f', ' --watch', ' --coverage')" },
        { "text": "The output contains test function definition syntax: 'def test_' or \"it('\" or \"describe('\"" },
        { "text": "The output contains code import syntax: 'import {' or 'from X import' or 'require('" },
        { "text": "The output contains variable declaration syntax: 'const ' or 'let ' or 'var ' followed by a name and '='" },
        { "text": "The output contains a file path with a line number reference in the format filename:NNN" },
        { "text": "The output contains test assertion code syntax: '.toBe(' or '.toEqual(' or '.toHaveBeenCalled('" },
        { "text": "The output contains a conventional commit message on its own line starting with 'feat:', 'fix:', 'chore:', 'refactor:', or 'test:'" },
        { "text": "The output contains test lifecycle hook syntax: 'beforeEach(' or 'afterEach(' or 'beforeAll('" },
        { "text": "The output contains an expected output prediction after a step (e.g. 'Expected output:' or 'You should see:')" },
        { "text": "The output contains 'npm run ' or 'yarn run ' or 'bun run ' or 'pnpm run ' as an explicit execution instruction in a step" },
        { "text": "The output contains at least one sentence with the word 'goal', 'intent', 'objective', 'purpose', or 'we want' describing the task's aim" },
        { "text": "The output contains at least one use of 'because', 'in order to', or 'so that' explaining why something matters" },
        { "text": "The output contains at least one completion criterion containing 'passes', 'verified', 'no remaining', 'confirmed', or 'complete when'" },
        { "text": "The output contains more than 4 distinct numbered tasks, sections, or top-level steps (indicating over-engineering for a simple rename)" },
        { "text": "The output contains at least one sentence describing what a successful outcome looks like rather than just what steps to perform" },
        { "text": "The output mentions external consumers, dependent packages, published APIs, or downstream callers as a concern" },
        { "text": "The output contains a specific search command as an instruction (e.g. 'grep -r', 'sed -i', 'find . -name', or a regex pattern)" },
        { "text": "The output contains a completion criterion about no remaining references to the old function name 'getUserData'" },
        { "text": "The output does not contain prescribed IDE or editor commands for performing the rename (e.g. 'use Find and Replace', 'use Rename Symbol', 'use VSCode refactor')" },
        { "text": "The output does not contain 'npm run ' or 'yarn run ' or 'bun run ' or 'pnpm run ' as an explicit execution instruction in a step" }
      ],
      "files": []
    }
  ]
}